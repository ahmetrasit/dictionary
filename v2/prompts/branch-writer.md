# Retired prompt: branch writer

Do not use this file as an agent prompt.

The branch-per-agent workflow is not part of v2 production. Current tasks are
root-level and use `v2/prompts/root-writer.md`. Do not resume or adapt a
historical task that names this prompt; regenerate a current task with
`v2/scripts/create_entry.py` and follow
`v2/orchestration/entry-creation.spec.md`.
