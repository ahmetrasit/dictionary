# Agent Dictionary Lookup

For root checks in this dictionary, the canonical agent entry point is:

[https://ahmetrasit.github.io/dictionary/agent/START_HERE.md](https://ahmetrasit.github.io/dictionary/agent/START_HERE.md)

If asked to check roots `x, y, z`, resolve each requested root through the static
agent access layer, open each candidate root card, and report every candidate
root ID inspected. The full root packets are source evidence, not the first
lookup surface.

Machine-readable descriptor:

[agent-access.json](agent-access.json)
